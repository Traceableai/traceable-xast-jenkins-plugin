# Custom plugin files need to be placed here.
# To apply these plugins update the config.yaml in ~/.traceable/
# If you are using docker based traceable-cli use /app/userdata/config.yaml
# Look at the provided sample config in how to apply the change